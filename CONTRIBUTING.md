# 代码规范与验证

本规范适用于正式标定代码、深度学习代码和配套测试。实验数据、权重和检查点不提交。

## 命名、格式与注释

- 文件、函数、变量使用 `snake_case`，类使用 `PascalCase`，常量使用 `UPPER_SNAKE_CASE`。
- `#` 后保留空格。注释解释选择原因、兼容限制和科学假设，不逐行复述代码。
- 公共函数使用 Google 风格 docstring：按实际需要写 `Args`、`Returns`、`Raises`、`Note`。
- 图像/数组接口写明 ndarray 或 Tensor、dtype、形状、通道、坐标顺序和单位。
- 返回 `None` 的函数不写空 `Returns`；异常说明必须与实现一致。
- docstring 存在性测试不证明说明正确；参数变更须同时审查注释和测试。

## 数值与坐标约定

| 对象 | 约定 |
|---|---|
| NumPy 灰度图 | uint8，`(H,W)` |
| OpenCV 彩色图 | uint8，`(H,W,3)`，BGR |
| OpenCV 图像尺寸 | `(width,height)` |
| 角点 | `(x,y)`，单位 px；常用数组 `(N,1,2)`，float32 |
| 棋盘规格 | `(columns,rows)`，物点按行排列，理想板 Z=0 |
| 平移/旋转向量 | mm / rad，旋转向量不是欧拉角 |
| 双目变换 | `P_right = R_RL @ P_left + t_RL`，左相机为基准 |
| 五参数畸变 | `(k1,k2,p1,p2,k3)` |

集中约定见 `src/contracts.py`。类型标注表达容器和数值类型，docstring 表达形状、
单位和语义，运行时检查验证输入；三者不能互相替代。复杂结果逐步使用 TypedDict，
OpenCV 类型存根不够精确时，仅在接口边界使用有依据的 `cast`。

## 接口、职责和文件写入

- 在数据读取、公共检测接口和模型检查入口验证空值、形状、有限性与正数约束。
- 内部函数可以依赖已验证的前置条件；不要在每次投影/优化迭代中重复检查完整模型。
- 错误包含实际值、期望值以及可获得的路径或样本编号；保留原始异常原因。
- 数据读取、检测、质量计算、绘图分别由函数负责；编排函数调用它们。
- 可调实验参数放入配置或 CLI；固定算法约定使用有意义的常量；不必命名每个数学数字。
- 检查图片写入返回值；检查点先写临时文件再替换，失败不得破坏旧检查点。
- 默认不覆盖已有实验目录。多个输出部分成功时明确记录失败，不伪造成功结果。

## 科学验证与回归测试

- 测试图像不参与相机标定、模型选择或调参；近重复组不跨验证折。
- 记录随机种子、划分、配置和模型来源；内部一致性不等于外部绝对精度。
- 测试正常输入、非法输入、缺图、保存失败、CSV 回读和权重读写。
- CPU 回归与 CUDA 检查分开；缺少依赖或设备导致的跳过不能算通过。
- 代码改动不得直接改写 `delivery/` 快照；更新交付结果需要单独核验来源。

## 提交前检查

在项目根目录、已安装锁定开发依赖的环境运行：

```bash
ruff check .
ruff format --check .
mypy src tests
pytest -q
pip check
python check_quality.py
python scripts/check_repository.py --history
git diff --check
```

`pyproject.toml` 和 `tests/conftest.py` 排除本地传统实验，与统一入口的交付范围对齐。
新增排除项时同步检查这些配置和 `scripts/check_repository.py`，不要借排除规则隐藏失败。
直接 mypy 命令检查 `src/tests`；统一入口额外检查 CLI、脚本，并执行 Bandit。
CI CPU 任务强制安装并导入 PyTorch，再执行训练模块回归；GPU 运行另行验证。

提交包含训练、推理及权重保存/加载代码，不包含权重数据、原始数据、缓存、临时文件。
CI 配置只能引用已纳入提交的文件。工作区测试和远端 CI 的状态应分别报告。
