## 变更目的

说明问题及变更后的行为。

## 验证

- [ ] `python check_quality.py` 通过
- [ ] `python scripts/check_repository.py --history` 通过
- [ ] 未包含 dataset、outputs、原始 BMP 或数据压缩包
- [ ] 坐标约定、CSV 或运行方式变化时已同步 docs

涉及数值算法时，说明合成真值测试和本地实测对比；不要将数据集附在 PR 中。
