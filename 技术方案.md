# 双目相机标定与棋盘格姿态估计技术方案

编制日期：2026-09-05（本地时区）。版本：v2，文献与开源实现融合版。状态：基于现有数据的实施设计；尚未运行标定或得到精度结果。

> 实施更新（2026-09-05）：现已完成三阶段代码、项目虚拟环境和 VS Code 配置。
> 确认棋盘为 12×9 内角点，23 对候选均检测成功；C0/C2 五折比较后选择 C2，
> 标定拟合 RMS 约 0.1948 px，5 对测试姿态均成功导出。没有新增确定异常，未额外执行 C1 删除。
> 本文后续“拟采用”“待实现”保留为原始设计记录；实际实现与运行方法以 README.md 为准，
> 最新数值及限制以 outputs/quality/quality_report.md 和对应 CSV/JSON 为准。

## 阅读导引与方案摘要

本方案面向现有棋盘格图像，采用“OpenCV 几何标定＋质量筛选＋双目联合位姿精化”的技术路线，不训练神经网络。原始标定集 26 对，预排除 000015、000016、000027 后剩余 23 对候选；5 对测试图像独立用于最终姿态输出。

技术依据分为四层：CameraCalibration 仓库提供角点提取与单目标定的工程参考；Kalibr 提供多相机联合几何和有效观测选择思想；OutAw 提供质量筛选与多指标验证框架；最优姿态引导论文提供姿态多样性和独立下游验证的依据。Processes 论文仅用于结果展示与质量报告的组织参考，不采用其定义有歧义的左右像素偏移指标。

阅读顺序：第 1–2 节明确任务和数据；第 3 节说明方法来源及融合路线；第 4–7 节定义几何与算法；第 8 节定义实验和验收；第 9–10 节定义工程交付；第 11–12 节给出参考资料和未决事项。文中“已核实”仅指已完成的文件/目视检查，“拟采用”均需后续实现验证。

## 1. 目标与范围

依据 `dataset/双目相机标定与基于双目相机的棋盘格姿态检测.pdf`，使用 Python 与 OpenCV 完成：

1. 从标定集估计左右相机内参、畸变及左右相机之间的旋转和平移。
2. 固定标定结果，对 5 对测试图像分别估计棋盘格相对左相机的姿态，利用右图共同约束并验证。
3. 交付可运行代码、依赖与使用说明、标定 CSV、含 5 条测试记录的姿态 CSV。

不要求训练神经网络、人工标注所有角点、场景稠密重建或鸟瞰图拼接。可输出棋盘格稀疏三维点作为质量验证。

## 2. 已核实的数据

项目根目录：`/Users/healy/Desktop/简历/learn-code-test/python/eyemove/Stereo Calibration`。

| 数据 | 左图 | 右图 | 配对情况 | 格式 |
|---|---:|---:|---|---|
| dataset/calibration_images | 26 | 26 | 编号 000001–000027，双方均缺 000012，无单侧缺图 | 1280×720，8 位灰度 BMP |
| dataset/pose_test_images | 5 | 5 | 编号 000001–000005，完整 | 1280×720，8 位灰度 BMP |

文件命名为 `left_pair_000001.bmp` / `right_pair_000001.bmp`。标定集与测试集存在相同编号，唯一键必须包含数据集名称。

### 已发现的图像内容异常（后续复核补充）

用户指出并经左右原图目视复核，`000015`、`000016`、`000027` 三对列入预排除清单：前两对的棋盘格画面分布在左右边缘，呈现疑似横向错位/回绕；第 27 对在约第 226 行出现明显上下画面断裂。具体采集或存储故障原因尚未确定，不将推测作为已证实结论。

后续实施默认按图像对整体排除上述三对，原因标记为 `suspected_image_discontinuity`；原始 26 对文件全部保留，不手工拼接、不删除。剩余 **23 对候选标定数据**仍需逐对审查并通过角点检测，不能称为 23 对已验证有效数据。测试集仍为 5 对，未因本次发现而变更。原始清点表的 26 对是磁盘库存数量，后续报告需区分库存、预排除及最终有效数量。

任务给定格子边长 **19 mm**。样图已目视检查，为普通棋盘格；**内角点行列数尚未通过自动检测确认**，不得沿用其他论文的 9×6 或仓库默认 7×6。文件可读、尺寸一致不代表角点检测一定成功。

当前检查使用的两个 Python 解释器均未安装 cv2；不能据此推断整台电脑均无 OpenCV。实施时建立项目独立环境并锁定实际验证过的依赖版本。

## 3. 总体流程

### 3.1 文献与仓库到项目模块的映射

| 来源 | 已阅读的相关内容 | 本项目借鉴 | 对应模块 | 不直接采用的部分 |
|---|---|---|---|---|
| [R1] dyfcalid/CameraCalibration | IntrinsicCalibration/intrinsicCalib.py 中的角点、单目求解、重投影；ExtrinsicCalibration/extrinsicCalib.py 中的 findHomography | 自动检测、亚像素定位、普通/鱼眼模型分离、角点叠加图 | corners.py、calibration.py | 不以 H 替代双目 R/T；不沿用其非标准 RMS 计算；不使用鸟瞰拼接 |
| [R2] Kalibr | 官方多相机标定、标定板、模型说明及 CameraCalibrator.py | 多相机共享几何约束、信息多样性、配置与报告可追溯 | calibration.py、pose.py、export.py | 不安装 ROS/IMU 依赖，不宣称复现信息论选帧，不改用 AprilGrid 数据 |
| [P1] OutAw | §3.2、§3.3、表 3/6/14/17 | 先评估再筛选、保留基线、多指标比较、重采样、三角化边长验证 | validation.py | 不照搬 25 mm 板、固定阈值、19 个六对候选或论文精度 |
| [P2] Optimal Pose Guidance | 方法、图 2、表 1–3、应变验证 | 保留板位置/距离/倾角多样性；评价不能仅靠训练残差 | validation.py | 不实施准直仪预标定、实时引导或高温 DIC；少图像外参结果不等同于少图像全参数标定 |
| [P3] Processes 标定评估 | §3.3、§4、图 11 | 每对质量统计、角点可视化、汇总报告 | export.py | 不将水平视差当标定错误，不取整后作为精度指标，不沿用不清晰的质量等级阈值 |

上述映射表示思想或工程参考，并非已完成源码移植。本文中的稳健统计规则、内部验证和测试双目位姿流程是面向本任务的组合设计，不属于任何一篇论文的原样复现。

### 3.2 融合后的处理流程

数据清点与按 ID 配对 → 确认棋盘格规格 → 自动角点检测和对应检查 → 单目标定初值 → 双目标定基线 → 标定集内部验证与必要的数据筛选 → 冻结参数 → 5 对测试图像姿态估计 → CSV 与可视化交付。

测试集不得用于选择相机模型、过滤阈值或调优标定参数；测试姿态求解只优化棋盘格位姿。

处理过程分为两个隔离阶段：标定阶段产出冻结的相机模型；姿态阶段只读取冻结结果。每一步均保存输入 ID、参数配置、状态和拒绝原因。错误帧预排除是基础数据有效性检查，所有对比方案使用相同清单，不能将“删除已知破损图片”的收益单独归因于新筛选算法。

### 3.3 算法原理与本项目取舍

已知棋盘格三维点 P_B 与检测像素 u，利用投影模型 π(K,D,P) 定义残差。单目标定先建立 K、D 初值；双目标定对全部选定图像共享同一个 R_RL、t_RL，各帧板位姿独立变化。这是联合几何约束，并不需要神经网络训练。[R1][R2]

标定问题的简化目标为：

```text
min Σ_i Σ_j [ ||u_Lij - π(K_L,D_L,R_LBi P_Bj+t_LBi)||²
             + ||u_Rij - π(K_R,D_R,R_RL(R_LBi P_Bj+t_LBi)+t_RL)||² ]
```

固定内参方案仅优化双目外参及各帧板位姿；联合方案还允许更新 K、D。已知 19 mm 间距提供尺度。不能用图像到图像的单应性矩阵直接替代这个多姿态、具有实际尺度的标定问题。[R1]

数据选择采用适合 23 对候选的简化流程：检测/编号有效性门控 → 初始标定 → 误差与覆盖诊断 → 少量明确异常排除 → 重新拟合和验证。暂不执行大规模随机子集搜索，避免在小样本上依赖复杂权重调参。[P1][P2]

## 4. 坐标系与单位

- 原始左相机坐标系 L 为默认参考：X 向右、Y 向下、Z 向前。
- 双目变换明确写为 `P_R = R_RL @ P_L + t_RL`，表示左相机坐标到右相机坐标。
- 基线为 `norm(t_RL)`；右相机光心在左系的位置为 `-R_RL.T @ t_RL`。
- 棋盘格局部坐标 B：以选定内角点为原点，列方向为 X，行方向为 Y，Z 按右手规则确定。
- 已知点为 `P_B(i,j) = (19*i, 19*j, 0)`，单位 mm。
- 测试姿态写为 `P_L = R_LB @ P_B + t_LB`。
- 旋转向量单位 rad，平移单位 mm，内参焦距与主点单位 px。旋转矩阵无量纲。
- 输出原始左相机系中的位姿，不混入立体校正后的虚拟相机系。

普通棋盘格可能存在方向编号歧义。每对图像必须保证左右索引对应同一物理角点。优先利用可见的板边缘/标记确认方向，并绘制角点编号；必要时比较符合棋盘拓扑的排序假设及其双目几何一致性。若无法跨帧确认唯一物理原点，使用明确的每帧编号约定并在输出中注明，不能声称已获得全局唯一板坐标系。

## 5. 角点获取与质量审计

### 5.1 数据读取

使用 pathlib 枚举文件并从文件名解析数字 ID，构建左右映射后按数字排序；禁止直接 zip 两个 os.listdir 的结果。检查图像解码、尺寸、重复内容与编号。中文路径可采用 `np.fromfile` 配合 `cv2.imdecode`。

保留原图和原始像素坐标，不默认裁剪、拉伸、去畸变后再进行首次标定。若为加速使用缩小图，只用于定位，最终角点应回到原始分辨率进行精化。

### 5.2 棋盘格规格确认

先目视数清完整板的内角点，再在多对清晰标定图上验证候选规格，确认全部网格及边界，防止把大棋盘的一块子区域误认作完整板。输出带编号的角点图后固定配置中的 columns、rows。

### 5.3 自动检测

本阶段参考 [R1] 的 findChessboardCorners/cornerSubPix 实现及 [P1] §3.3.3。当前数据是单通道灰度图，不能直接照搬仓库中无条件 BGR2GRAY 的代码；读取后按通道数处理。所谓“打标”在这里是自动生成二维角点与已知三维网格的对应，不生成 CVAT/LabelMe 人工训练标签。

优先试用 `findChessboardCornersSB`；必要时回退到 `findChessboardCorners` 加 `cornerSubPix`。SB 已提供亚像素角点，不机械地再次精化；以标定集内部验证决定是否有收益。

传统检测精化初始配置：窗口 (5,5)，zeroZone=(-1,-1)，最大 100 次，epsilon=1e-4；这是本项目拟用配置，不是已验证的最佳值。

仅左右均成功、完整且编号一致的图像对进入双目标定。记录检测器、失败原因、角点数、板覆盖率、角点包围框、板中心及板附近清晰度。检测失败应显式上报，不填造角点。

## 6. 相机标定

### 6.1 基线模型

先采用普通针孔＋五参数径向/切向畸变：`D=(k1,k2,p1,p2,k3)`，分别以 `calibrateCamera` 求左右 K、D 和每帧板位姿。图片本身不能证明镜头类型；如后续发现明显模型不匹配，再比较鱼眼模型。当前最多 23 对候选数据不默认启用高阶 rational/thin-prism 模型。

以单目结果初始化 `stereoCalibrate`，先固定内参求 R_RL、t_RL；再把允许内参联合优化作为候选方案。通过标定集内部留出误差、稳定性和物理合理性选择，不能仅按训练 RMS 最小选择。

### 6.2 数据质量改进

保留全有效图像基线。检查每对重投影误差、校正后垂直误差、覆盖率及姿态分布。采用中位数/MAD 等统计量标记异常，结合角点图复核；逐步剔除确定异常对，再重新标定并比较。

该阶段借鉴 [P1] 的“先有初值，再计算几何质量”顺序。指标初始定义：覆盖率=角点包围框面积/全图面积；清晰度在板附近区域统计拉普拉斯响应方差，同时记录区域以保证可比性；几何误差记录均值、中位数、P95。左右投影大小可能因相机焦距和板倾角自然不同，因此 LR 尺寸差仅作诊断特征，不照搬论文的硬阈值。

可采用 `median + 3×1.4826×MAD` 作为非负误差指标的初始异常提示线，MAD=median(abs(e-median(e)))。它是本项目工程初值，不是论文原公式，也不自动触发删除；MAD 接近零时改为人工复核并报告退化情况。清晰度低但几何观测可靠的帧不能仅凭单个分数排除。

不直接套用 OutAw 的 40、0.04、1.0 px 阈值，也不为了降低 RMS 删去全部大倾角、边缘或较远图像。默认最多两轮筛选；若需移除超过有效图像的 20%，先重新诊断板规格、编号和模型。该比例仅为工程审查触发点。

借鉴 Kalibr 的观测信息多样性思想：检查画面位置、板面积、距离及倾角覆盖；本项目不宣称复现其信息论选择算法。

### 6.3 验证策略

使用固定随机种子的 5 折标定集交叉验证（有效数据足够时），同一图像对不可拆分，近重复采集尽量分组分折。先固定预排除和角点可用性规则，再构建所有方案共用的折；每折的初始标定、统计阈值、几何筛选和重新标定仅使用该折训练对。不能先用全体数据的标定残差筛选，再对筛选结果交叉验证。各方案在相同可检测的留出对上评分，不因留出误差高而删去评测对。

对留出对用左图估计板姿态，再投影右图评估跨相机预测误差，同时记录双目位姿拟合残差；后者已使用双方观测，不能与前者混为一个独立预测指标。

方案确定后，使用最终保留标定对重新求最终参数；记录训练、留出和最终拟合指标的不同口径。可补充约 20 次重采样估计基线、焦距变化，仅解释为经验稳定性，不冒充外部真值精度或严格置信区间。

## 7. 五对测试数据的姿态估计

本阶段是题目要求的补充实现。现有 CameraCalibration 仓库的 H 分解并非完整测试位姿流程；OutAw 的主要任务也是标定质量，不能直接视为已有五组姿态答案。这里借鉴 [R2] 的共享双目变换与 [P2] 的 PnP 初值/联合几何思想，独立实现固定相机参数后的板位姿优化。

1. 使用冻结的棋盘规格、相机模型与 K、D、R_RL、t_RL 检测测试角点。
2. 用 `solvePnPGeneric` 的平面 IPPE 求候选位姿，或使用适合平面板的 PnP 初始化。棋盘格多角点不使用仅适合特定四点排列的 IPPE_SQUARE。
3. 对候选进行左右相机正深度检查、重投影比较和角点排序检查；平面双解若分数接近，记录歧义而非默默选取。
4. 固定全部相机参数，仅优化板的 6 自由度位姿，联合最小化左右原图角点重投影误差。可用 SciPy least_squares 与 OpenCV projectPoints，实现双目联合位姿精化。
5. 右侧板位姿由 `R_RB=R_RL @ R_LB`、`t_RB=R_RL @ t_LB+t_RL` 得到。
6. 输出原始左右图上的坐标轴叠加、每侧 RMS、正深度与歧义状态。五个测试 ID 均必须有记录；失败记录留空数值并填写原因，不能静默漏行。

姿态目标函数的简化形式：`sum_j(||u_Lj-project_L(R_LB*P_Bj+t_LB)||² + ||u_Rj-project_R(R_RL*(R_LB*P_Bj+t_LB)+t_RL)||²)`。如使用鲁棒损失，另行报告不加权原始 RMS，便于比较。

## 8. 质量指标与验收

### 8.1 统一评价指标

| 检查 | 定义与要求 |
|---|---|
| 每角点重投影 RMS | sqrt(sum(||观测-预测||²)/N)，N 为二维角点观测数；左右合并时分母包含双方 |
| 极线误差 | 对水平立体校正后的对应角点统计 abs(yL-yR) 的均值、中位数、P95；不把水平视差当标定误差 |
| 三维边长 | 去畸变并在一致坐标模型下三角化，比较水平/垂直相邻角点距离与 19 mm，不混入对角线 |
| 物理一致性 | fx、fy 为正，R.T@R 接近 I、det(R) 接近 1，参数有限，板点在两相机前方 |
| 数据与格式 | 配对可追溯；最终测试 CSV 恰好 5 行；CSV 回读可还原参数并重算投影 |
| 可复现性 | 固定配置、依赖、随机种子、运行命令与保留/剔除列表 |

参考工程目标：单目/双目重投影 RMS 和平均极线误差尽量低于 1 px；0.5 px 可作为更高目标。它们不是题目给定的评分线，也不是预先承诺。未达到时报告实测结果与诊断，不人为更改指标。

已知板边长可验证几何一致性，但没有外部位姿或基线真值时，不能声称实际位置误差达到某个毫米级别。数据噪声、标定板不平整和真实格子尺寸偏差可能形成系统误差。

### 8.2 对比实验编排

以下实验借鉴 [P1] 的基线/改进对比和 [P2] 的多维验证，但名称为本项目定义，不称为原文 BSC、InterFil、OutAw 的复现。候选数据、初始模型、检测缓存、划分和评价口径保持一致。

| 实验 | 唯一主要变化 | 回答的问题 | 输出 |
|---|---|---|---|
| C0 基线 | 全部可用训练对，单目初值＋固定内参双目标定 | 不做几何筛选能达到什么水平？ | 每折和最终参数、残差 |
| C1 筛选 | 在 C0 基础上增加训练折内部异常审查与筛选 | 数据质量控制是否改善留出表现？ | 拒绝原因、数量、相同留出对误差 |
| C2 联合 | 在 C1 相同保留对上允许内参联合优化 | 额外自由度有收益还是过拟合？ | 留出误差、内参/基线变化 |
| P0 左目姿态 | 最终相机参数固定，测试图使用左图 PnP | 位姿初始化质量如何？ | 左侧拟合、右侧预测误差 |
| P1 双目姿态 | 与 P0 相同初值/观测，固定相机，仅联合优化板位姿 | 使用右图后双方一致性是否改善？ | 双方及联合 RMS、歧义状态 |

C2 是可选实验；C1 不优于 C0 时保留 C0。P1 对两侧训练残差的降低只说明拟合一致性，未有真实板姿态时不能据此断言绝对位姿精度提升。位姿输出不反向用于挑选 C0/C1/C2。

### 8.3 结果表与可视化清单

正式报告先展示数据流转表（26 原始→3 预排除→23 候选→检测成功→最终保留），再展示 C0/C1/C2 的同口径比较，最后逐一展示 5 对测试姿态。结果表预留字段但不填写推测数值：

| 方案 | 训练对数 | 留出对数 | 训练 stereo RMS/px | 留出右图预测 RMS/px | 留出极线 P95/px | 基线/mm | 状态 |
|---|---|---|---|---|---|---|---|
| C0 | 待运行 | 待运行 | 待运行 | 待运行 | 待运行 | 待运行 | 未执行 |
| C1 | 待运行 | 待运行 | 待运行 | 待运行 | 待运行 | 待运行 | 未执行 |
| C2（可选） | 待运行 | 待运行 | 待运行 | 待运行 | 待运行 | 待运行 | 未执行 |

建议图件：角点编号叠加、预排除帧示例、逐对误差图、板中心/面积/姿态覆盖图、立体校正水平参考线图、相邻角点边长偏差图，以及测试左右坐标轴图。[P1][P2][P3]

重采样报告需包含成功/失败次数、抽样单位（整对）、是否放回及随机种子。重复导致独立姿态不足或求解失败时计入失败率，不悄悄丢弃后仅汇报漂亮的标准差。

## 9. 工程结构与交付格式

建议后续新增下列结构，现阶段仅创建本方案：

```text
Stereo Calibration/
  dataset/                       # 原始题目和数据
  技术方案.md
  README.md
  requirements.txt
  config.yaml
  run_pipeline.py
  src/
    dataset.py                 # 清点、配对、解码
    corners.py                 # 规格确认、检测、编号检查
    calibration.py             # 单目/双目求解
    validation.py              # 内部验证、筛选、几何质量
    pose.py                    # 测试位姿初始化与联合精化
    export.py                  # CSV、JSON、可视化
  tests/
  outputs/
    calibration_results.csv
    pose_results.csv
    calibration.json
    pair_manifest.csv
    quality_report.md
    corners/
    rectified/
    poses/
```

标定 CSV 使用长表结构：`group,parameter,row,col,value,unit,convention`。保存左右 K、D，R_RL、t_RL，基线、图像尺寸、格子尺寸、内角点规格、最终有效对数与 RMS；D 的顺序、模型名称及变换方向明确记录。JSON 保存完整数组和运行元信息，CSV 为题目要求的正式结果。

姿态 CSV 每个测试对一行，字段建议：`pair_id,status,reference_frame,board_origin_convention,rx_rad,ry_rad,rz_rad,tx_mm,ty_mm,tz_mm,r11,r12,r13,r21,r22,r23,r31,r32,r33,left_rms_px,right_rms_px,joint_rms_px,ambiguity_flag,failure_reason`。其中 rvec 与 R 表示同一旋转，导出后检查相互一致；默认不输出约定易混淆的欧拉角。

建议依赖：Python 3.10/3.11、opencv-python-headless（若交互显示则改用 opencv-python，二者不并装）、numpy、scipy、PyYAML、matplotlib。具体版本安装验证后锁定。

## 10. 分阶段实施与验证

1. 数据阶段：确认板规格、输出配对清单与所有角点叠加图，明确失败数量。
2. 基线阶段：完整运行单目、双目标定，保存全有效对基线，报告实测误差。
3. 改进阶段：完成内部交叉验证、必要的筛选和模型对比，冻结最终参数。
4. 姿态阶段：处理 5 对测试图，输出左右坐标轴图、姿态与异常状态。
5. 交付阶段：在干净环境按 README 重跑；验证 CSV 回读、所有测试 ID、单位和坐标变换。

重点测试：缺失/错配 ID、灰度图读取、非连续编号、旋转矩阵逆变换、合成已知位姿的双目重投影、平面双解、重投影 RMS 公式，以及失败记录不会丢失。不得通过重写真实测试结果来满足测试。

## 11. 参考方法与复用边界

### 11.1 开源工程与接口

- **[R1] dyfcalid/CameraCalibration**：[仓库](https://github.com/dyfcalid/CameraCalibration)、[单目标定源码](https://github.com/dyfcalid/CameraCalibration/blob/master/IntrinsicCalibration/intrinsicCalib.py)、[平面映射源码](https://github.com/dyfcalid/CameraCalibration/blob/master/ExtrinsicCalibration/extrinsicCalib.py)。内参脚本使用自动角点检测和 OpenCV 标定；外参脚本核心为 findHomography，并非 stereoCalibrate。许可标记为 GPL-3.0；当前仅阅读参考，后续如移植代码需保留来源与适用许可记录。
- **[R2] ethz-asl/kalibr**：[仓库](https://github.com/ethz-asl/kalibr)、[多相机标定](https://github.com/ethz-asl/kalibr/wiki/multiple-camera-calibration)、[支持模型](https://github.com/ethz-asl/kalibr/wiki/supported-models)、[标定板](https://github.com/ethz-asl/kalibr/wiki/calibration-targets)。Kalibr 棋盘尺寸配置单位为 m，交叉验证时 19 mm 对应 0.019 m；其 radtan 配置与 OpenCV 默认五参数模型需明确匹配，不直接逐项比较不同模型系数。
- **[R3] OpenCV calib3d**：[官方接口](https://docs.opencv.org/4.x/d9/d0c/group__calib3d.html)。负责角点检测、单目/双目标定、PnP、投影、校正和三角化。后续冻结实际安装版本及参数 flags。

### 11.2 论文

- **[P1] Carbureanu, M.; Zamfir, F.-S.** Quantifying and Improving Stereo Camera Calibration Robustness: An Outlier-Aware Algorithm for Digital Twin Data Acquisition. *Journal of Imaging*, 2026, 12, 280. [DOI](https://doi.org/10.3390/jimaging12070280)。依据用户提供的 `jimaging-12-00280-v2.pdf`；重点 §3.2–3.3、表 3/6/14/17。全文包括两组采集实验；配置与实际子集数量存在差别，不将其作为本项目硬编码依据。
- **[P2] Tan, D. et al.** Optimal Pose Guidance for Stereo Calibration in 3D Deformation Measurement. *Experimental Mechanics*, 2026, 66, 501–514. [DOI](https://doi.org/10.1007/s11340-025-01255-1)。依据 `s11340-025-01255-1.pdf`；重点正文第 3–8 页及表 1–3。方法以准直仪获得的精确内参为前提；仿真 5 mm 格距不可移用于本任务。主文提及的补充附录未包含于已读 PDF，不宣称已核对完整实现。
- **[P3] Ivascu, L.; Vinatu, V.-F.; Gaianu, M.** An Assessment of the Stereo and Near-Infrared Camera Calibration Technique Using a Novel Real-Time Approach in the Context of Resource Efficiency. *Processes*, 2025, 13, 1198. [DOI](https://doi.org/10.3390/pr13041198)。依据 `processes-13-01198-v2.pdf`；重点 §3.3、正文第 7–10 页。仅借鉴报告组织；取整角点、水平/垂直偏移混述、质量分级阈值不作为算法依据。
- **[P4] Zhang, Z.** A Flexible New Technique for Camera Calibration. *IEEE TPAMI*, 2000, 22(11), 1330–1334. [作者机构页面](https://www.microsoft.com/en-us/research/publication/a-flexible-new-technique-for-camera-calibration/)。提供平面标定基础；双目外参仍需利用左右对应与共同几何约束。

此前讨论的 LiDAR–camera 综述涉及异构传感器标定，不纳入本项目核心算法依据；SoccerNet/双目匹配竞赛资源也不作为棋盘格标定的直接实现基线。

### 11.3 可追溯性规则

实现 README 应分别列出“思想参考”“调用库”“实际复用代码”。如后续下载或移植仓库代码，记录 commit、文件路径、修改点和许可；本次文档整理未下载或移植代码。论文的原始结果只能在相关工作中作为来源说明，不能填入本项目实验表。本文源码观察来自此前会话已读取的版本，不等同于检查未来的仓库更新。

## 12. 待实现确认项

内角点 columns/rows、全数据角点成功率、镜头模型适配性、编号方向是否能跨帧唯一识别，以及可达到的误差均待实际运行确认。无时间戳/触发元数据时，文件配对只能证明编号对应，不能证明硬件同步。方案中的阈值、折数、优化次数均是可调整的工程初值；调整依据只来自标定集。
